 function rediansToDegrees(input) {
    const radiаn = Number(input[0]);
    const degrees = (radiаn * 180) / Math.PI;
 console.log (degrees);
 
 }
 
 rediansToDegrees (["3.1416"]);
 rediansToDegrees (["6.2832"]);